version: '3.9'
services:
  mysql:
    image:  mysql:latest
    container_name: mysql_container
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: orderdb
    ports:
      - "3306:3306"
    volumes:
      - ./mysql:/var/lib/mysql


